'use client';

import React from 'react';

// Phase 0: Stub provider
// Phase 2 will add WagmiConfig, RainbowKit, and wallet integration
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
